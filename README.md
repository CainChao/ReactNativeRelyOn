# react-native-scrollable-tab-view是一个带Tab的ViewPager

#添加react-native-scrollable-tab-view依赖在doc命令中进入项目的根目录执行npm install react-native-scrollable-tab-view --save

#react-native-tab-navigator在Android中类似FargmentTabHost组件
#添加react-native-tab-navigator依赖在doc命令中进入项目的根目录执行npm install react-native-tab-navigator --save